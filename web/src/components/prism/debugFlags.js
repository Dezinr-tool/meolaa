/**
 * Temporary prism-section debug switches.
 * Flip these back when shape positioning is done.
 */

/** Pause VolumetricBeam / DispersionRibbon / ContactFlare + ScrollTrigger pin/scrub. */
export const PAUSE_LIGHT_AND_SCROLL = true

/**
 * Section + WebGL clear color while positioning.
 * Set to `null` to restore the production near-black look.
 */
export const PRISM_SECTION_BG = '#e8e8ec'
