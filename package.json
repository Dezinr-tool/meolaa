{
  "name": "meolaa",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web",
    "prototype": "npx --yes serve prototype -p 3000",
    "dev:next": "npm run dev --prefix next"
  }
}
